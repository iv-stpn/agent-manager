import type { Db } from "../client";
import { type NewMessage, type NewToolCall } from "../schema";
export declare function insertMessage(db: Db, data: NewMessage): {
    id: string;
    error: string | null;
    createdAt: number;
    sessionId: string;
    role: "user" | "assistant";
    content: string;
    inputTokens: number | null;
    outputTokens: number | null;
    cacheReadTokens: number | null;
    cacheWriteTokens: number | null;
    errorDetails: string | null;
};
export declare function getMessages(db: Db, sessionId: string): {
    id: string;
    error: string | null;
    createdAt: number;
    sessionId: string;
    role: "user" | "assistant";
    content: string;
    inputTokens: number | null;
    outputTokens: number | null;
    cacheReadTokens: number | null;
    cacheWriteTokens: number | null;
    errorDetails: string | null;
}[];
export declare function insertToolCall(db: Db, data: NewToolCall): void;
export declare function completeToolCall(db: Db, id: string, output: string, status: "success" | "error"): void;
export declare function getToolCalls(db: Db, sessionId: string): {
    id: string;
    status: "error" | "pending" | "success";
    createdAt: number;
    sessionId: string;
    messageId: string;
    toolName: string;
    toolUseId: string;
    input: string;
    output: string | null;
    completedAt: number | null;
}[];
